# Copiez ce fichier en `.env` puis remplissez les valeurs.
# Le `.env` est ignoré par git : NE JAMAIS committer de secrets.

# Web client ID OAuth (console Google Cloud > Identifiants OAuth, type "Web")
# Sert au login Google via Firebase Auth.
FIREBASE_WEB_CLIENT_ID=126002780914-xxxxxxxxxxxxxxxx.apps.googleusercontent.com
